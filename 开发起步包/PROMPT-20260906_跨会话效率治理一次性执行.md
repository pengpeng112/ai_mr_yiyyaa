# PROMPT — 20260906 跨会话效率治理一次性执行（Skill/脚本/指令治理/卡点/回归）

> 交接日期：2026-09-06 ｜ 委托：用户 ｜ 起草：ZCode/GLM-5.3 ｜ 作业书版本：**043 v1.1**（外部 AI 复核 18 条全采纳修订版，**按 v1.1 执行，不要按 v1.0**）
> 你的身份：一次性执行 AI。按本提示词 + **`docs/ACTIVE/043` v1.1（唯一作业书）** 一口气做完，交付=044。

## 0. 必读顺序

`AGENTS.md` → `开发起步包/README.md` → `开发起步包/00_AI协作规则.md` → `开发起步包/01_统一修改记录.md` 末 10 行 → `docs/INDEX.md` → **`docs/ACTIVE/043` v1.1 全文（认 §0 修订记录）** → 本提示词其余部分。

分析素材（已备好，勿重建）：`review/conversations-export-20260906/`（13 会话+INDEX，**证据边界=仅 ZCode 执行会话**，Grok/Codex 缺口在 044 记「未纳入」）。

## 1. 基线（2026-09-06 定格）

- 分支与 origin 同步；**工作区=043 起草现场，不是干净的**：已知未提交=docs/INDEX.md、开发起步包/README.md、01_统一修改记录.md（M）+ 043 作业书、两份 PROMPT-20260906、scripts/export_zcode_conversations_20260906.py（??）。**全部保留，禁止 reset/clean/stash。**
- 门禁锚：主 **1295** / prearchive **283+1skip** / unit **57** / e2e **52+17skip**（首版机器源 `docs/reference/gate_anchors.json` 随 R2 落地）。
- 零生产；生产 BFF=503、预检进程未起、全开关关——本轮一概不动。

## 2. 本轮一句话

把 13 个历史会话中反复出现的流程沉淀成 3 个 Skill + 3 个脚本（含首版锚 json），修正指令文件 5 处冲突（C1 按主题行定位**禁止行号**），产出卡点台账与规则取舍表，并用聚合门禁脚本真机重跑全量回归（含可选 legacy 规则中心 E2E）。

## 3. 已裁定（禁止再争论）

1. 只做 043 §4 的 R1-R5；Skill 恰好 3 个且 **description 必须含触发词**；门禁聚合唯一入口 `scripts/run_gates_20260906.py`。
2. **不创建任何定时任务**——只交付建议清单等批准。
3. C1 修正口径以 AGENTS.md Dual-Mode 节（2026-09-01 核验）为准，**按功能状态表主题行「双模式与终末覆盖」定位**；CLAUDE/00/AGENTS **只指针化不删除**。
4. 锚单一来源=`docs/reference/gate_anchors.json`（R2 落地首版；run_gates 未传 `--anchor-file` 或文件不存在**只出表不比锚**）；101 只加指针段；**带日期的历史文档数字一律不改写**（023/020/036-042 正文禁区）。
5. 业务代码零改动（app/、prearchive_service/prearchive/、rules JSON、static/、frontend/src/ 全禁区）；测试只增不减；**023 仅 §0.6 追加一行**（041 先例）。
6. `--quick`=compileall(**含 prearchive_service**)+naming+isolation+sidecar --check（分钟级）；`--full`=全部+可选 legacy 规则中心 E2E（18080+18600 都在听才跑，否则表标 `SKIPPED(no-demo)` 并在 044 声明）。compileall 口径以 041 §8 为准，**不要抄 AGENTS Commands 那条**（它缺 prearchive_service）。
7. 端口清理：默认可杀=18080-18082/18600；**4173 只报不杀**（Playwright preview）；netstat 同时认 `LISTENING`/`侦听`；默认 dry-run。
8. R4 守卫=`tests/conftest.py` autouse env 快照/恢复；env 审计空窗不算失败；卡点台账十条种子必须落地、追加全带证据**不凑数**。
9. 停机条件：**包红即停**；根因是作业书缺陷 → 升级出口问用户，禁止现场改口径硬过。
10. 用户未批准不 commit/push。

## 4. 执行顺序

R1（3 Skill+仅静态测试）→ R2（run_gates/clean_ports/update_anchor+**首版 gate_anchors.json**+测试，`--quick` 真机验证）→ R3（C1-C5 指令治理+023 §0.6 追加行+断言）→ R4（卡点台账+conftest 守卫+env 审计附录）→ R5（`run_gates --full` 全量【预期数十分钟，以脚本最终 Markdown 表为准】+规则取舍表）→ 044 交付报告 → INDEX/01 登记。

每包测试绿再进下一包；checkpoint 写 `review/exec-log.md`。

## 5. 门禁（仓库根）

```
python scripts/run_gates_20260906.py --full
```

0 failed；计数不低于 §1 锚；isolation 零 `import app.*`；oneshot 默认 skip 保持；legacy 规则中心 PASS 或 `SKIPPED(no-demo)`+044 声明。

## 6. 升级出口（停下问用户）

须改业务代码/生产才过门禁；须删除指令文件内容而非指针化；锚低于基线且根因在既有三包；要现在创建定时任务；要现在催办外部材料类卡点；**作业书缺陷致包红**。

## 7. 完成定义

043 §10 勾选全过 + `docs/ACTIVE/044_043_EXECUTION_DELIVERY_REPORT_20260906.md`（**文件名钉死**；含卡点台账/规则取舍表/门禁表/C1-C5 前后对照/未启用 cron 声明/env 审计附录/Grok-Codex 未纳入声明）+ INDEX 043/044 行 + 023 §0.6 追加行 + 01 登记。未批准不 commit。
