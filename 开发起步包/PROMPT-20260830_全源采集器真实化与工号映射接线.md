# PROMPT — T8 全源采集器真实化 + HIS基本信息工号映射接线（一次性开发交接）

> 交接日期：2026-08-30 ｜ 委托：用户 ｜ 起草：ZCode(GLM-5.3)
> 你的身份：一次性开发执行 AI。本提示词自包含全部实测数据，**按包推进一口气完成**；与 031 冲突时以 031 为准，本提示词为其 T8-2/T8-3 的"实测数据解堵版"。

## 0. 必读顺序

`AGENTS.md` → `开发起步包/README.md` → `00_AI协作规则.md` → `01_统一修改记录.md` 末 10 行 → `docs/INDEX.md` → 本提示词全文 → 按需 `docs/ACTIVE/031`（§3 T8 原始规格）、`029`、`032`（交付报告）、`033`（请求二办结注记）。

## 1. 当前基线（2026-08-30 实测，直接以此为准，勿重复发现）

- 分支 `fix/ora-12609-p4-error-code`，HEAD `2407bc0`（031 已执行完毕、生产已热更部署）；
- 主服务 pytest **1176 passed / 0 failed**；prearchive pytest **196 passed / 1 skipped**；check_isolation 通过（46 个 py）；
- 工作区脏文件：`docs/ACTIVE/033_*`（M）、`docs/INDEX.md`（M）、`开发起步包/01_统一修改记录.md`（M）——T0 第一笔提交收口；
- `docs/docs/`（未跟踪）= 用户截图目录，**已登记勿提交**，你负责在 P-D 把 `docs/docs/` 加进 `.gitignore`。

## 2. 关键实测事实（2026-08-29 平台侧已核验，直接使用，禁止再连真实库重测）

### 2.1 五源连接状态（数据资产平台已登记，source_code 可用）

| 源 | source_code | 库/实例 | 状态 |
|---|---|---|---|
| 病理 | `pathology_sqlserver_10_10_9_41` | SQL Server，库 `pitaya`（**已修复**：原注册库名误填"pitaya,QPIS"双库串；需 TDS 7.0，连接器已内置回退） | ✅ 端到端通 |
| 血透 | `dialysis_postgresql_10_10_10_88` | PostgreSQL，库 `dialysis` | ✅ |
| 电测听 | `hearing_test_postgresql_10_10_9_187` | PostgreSQL:15432，库 `report` | ✅ |
| 气管镜 | `bronchoscopy_postgresql_10_10_8_240` | PostgreSQL，库 **`clouddb`** | ✅ |
| HIS基本信息 | `hisbase_oracle_10_10_10_14` | Oracle，**service_name=`hisserver`**（不是 hissuer） | ✅ |

### 2.2 四源接口表真实列结构（T8-2 回填依据）

**病理 `pitaya.dbo.T_ITF_BL`（13 列标准骨架，192,653 行）**：
FID/PATIENTID/FBIHID/FBINCU/REPORTNAME/FDESC/PDFNAME/PDFPATH/FCKDATE/FUPDATE/FLOADDATE/FREPORTSTYLE/PAGECOUNT

**气管镜 `clouddb.public."T_ITF_HisQuery"`（13 列，553 行，REPORTNAME 全部=呼吸内镜检查报告）**——注意表名大小写敏感必须带引号：
FID(text)/FBIHID(text)/**FBINCU(integer)**/PDFPATH/PDFNAME/FCKDATE(**timestamp**)/FLOADDATE(timestamp)/REPORTNAME(text)/FDESC/PATIENTID(text)/PAGECOUNT(integer)/FREPORTSTYLE/**FUPDATE(timestamp)**

**血透 `dialysis.public."T_ITF_XT"`（14 列 = 标准13 + IDNo，1,442 行）**：
FID(**bigint**)/PATIENTID(**bigint**)/FBIHID(varchar)/FBINCU(integer)/REPORTNAME(text)/FDESC/PDFNAME/PDFPATH/FCKDATE(**timestamptz**)/FUPDATE(timestamptz)/FLOADDATE(timestamptz)/FREPORTSTYLE/PAGECOUNT(integer)/**IDNo(varchar——身份证号列，PHI 红线：任何 fixture/样例/日志不得含真实值，适配层可整列丢弃）**

**电测听 `report.t_itf_report`（16 列小写命名，118,776 行）**：
fid/patientid/fbihid/fbincu(全 varchar)/reportname/fdesc/pdfname(text)/pdfpath/**report_url(text 新增列)**/fckdate/fupdate/floaddate(全 timestamp)/freportstyle/pagecount(integer)/**his_patient_id(varchar 新增)**/**visit_index(varchar 新增——疑似住院次映射，可在注释标注待 W9 核对)**

### 2.3 HIS基本信息三视图真实结构（T8-3 依据，2026-08-29 实测采样）

- `hisuser.VW_user_info`（4,278 行，7 列）：**FID=工号**（实测格式 000201）/ **FNAME=姓名** / **FDEPT=科室代码** / **FDOCT=科室名称**（实测值是"口腔科/病案室"等——不是医生标志位！按此语义映射）/ FPOSITION=职务 / FUSERTYPE=用户类型
- `hisuser.VW_dept_dict`（816 行，6 列）：FID/FNAME/FQUN/FBETO/FTYPE/FBQNT（FID 疑似科室代码、FNAME 科室名，需采样验证与 VW_user_info.FDEPT 的 join 命中率）
- `hisuser.VW_pats_out_hospital`（62,885 行，53 列）：FPATIENTID/FBIHID/FBINCU/FNAME/FIHDAT/FOOFFI/**FGUIDANGDATE(归档日期)**/**FLEVWAY(离院方式)**/FISSURGERY/FSURGERYCODE/FTRANSFUSION 等——本期只做文档记录（未来出院触发交叉校验/手术触发证据的候选源），**不新增触发实现**

### 2.4 其他既定口径

- RPA 过渡锚点（anchor_mode=paperless_rpa）用户已拍板启用（≈出院后 5 天）；RPA 表 COMPLETED 恒 1 无区分度，增量信号=UPDATEAT；该模式 require_source_ready 门禁用（代码已实现）；
- W10 系统推送类规则清单**用户尚未提供**——`rules/system_push_rules.json` 保持零真实规则，**本单禁止写入任何具体报告规则**；
- 质控科免签字授权已落地：14 条正式规则 v`2026.08.29-qc-authorized-v1` 已回填 FID——**不要动这些正式规则**。

## 3. 任务包（P-A → P-D 按序，每包独立提交）

### P-A 四源采集器真实化（T8-2 收尾）

**目标**：病理/气管镜/血透/电测听四个 BLOCKED 骨架升级为真实列结构实现，TODO/skipif 占位转正。

**步骤**：
1. `collectors.py`：四个 `Sql*Gateway` 的 ITF_SQL 按 §2.2 真实列名/表名/schema 回填（气管镜表名带引号 `"T_ITF_HisQuery"`；电测听小写列名靠现有 lowercase 归一天然兼容；病理 MSSQL 记得 TDS 版本由生产连接配置负责——代码注释标注"该源需 TDS 7.0"）；删除对应 TODO/BLOCKED 注记；
2. 类型差异适配：FBINCU/PAGECOUNT/PATIENTID 数值型源（气管镜/血透）在身份映射处统一 `str()`（现 identity 三元组转 str 已有基础，确认覆盖）；timestamp 列直接 parse；血透 `IDNo` 列**不进入** DocumentEntry.raw 之外的任何输出（或在适配层置 None）；
3. `fixture_sources.py`：四源 fixture 行改为真实列名结构（虚构 TEST 患者值；血透 IDNo 用假号）；
4. 测试：BLOCKED 恒 skip 的占位测试删除，换成实质适配断言（真实列 fixture 行经 adapt_itf_rows 产出正确 DocumentEntry）+ builder e2e（四源条目进入 documents 且 source_watermarks 覆盖）；
5. `config.example.json`：四源 enabled=false、DSN 占位保持——**真实连接只发生在生产受控配置，仓库零真实 DSN**。

**验收断言**：□ 四源 Gateway 无 TODO/无 "标准骨架占位" 字样（文本断言）；□ 四源 fixture 适配单测+builder e2e 绿；□ 血透 IDNo 不出现在任何测试断言/文档样例中；□ 原 196+新增全绿；□ check_isolation 过。

### P-B HIS基本信息接入（T8-3）

**步骤**：
1. `collectors.py` 增 `HisBaseGateway`（抽象+Sql 实现+fixture）：`fetch_user_info()`（全量 4,278 行级）、`fetch_dept_dict()`（816 行）、`fetch_out_hospital_sample()`（可限量）；
2. `receivers.py` 增 `HisBaseUserIdMapper`（按 VW_user_info.FID 工号查映射；查无返回 None 下探兜底链）；config 开关 `receiver.userid_mapper_source: passthrough(默认) | hisbase`——**默认 passthrough 完全不改变现状**；
3. 科室规范化：`HisBaseDeptNormalizer`（VW_dept_dict 代码→名称），供规则 dept_codes 匹配与推送文案使用（同样配置开关，默认关）；
4. 采样验证（sjzc 只读聚合，已授权范围）：验证 `VW_user_info.FDEPT` 与 `VW_dept_dict.FID` join 命中率（`SELECT COUNT(*) 总数, COUNT(d.FID) 命中 FROM VW_user_info u LEFT JOIN VW_dept_dict d ON u.FDEPT=d.FID`），结果写入交付报告；**禁止拉取人员明细清单落盘**；
5. `VW_pats_out_hospital`：仅在 README/031 衔接表记录为未来候选源（归档日期触发/手术触发证据），本期零实现；
6. 测试：gateway fixture 单测、mapper 三态（命中/未命中走兜底/开关关闭不查）、normalizer 映射单测。

**验收断言**：□ 默认配置下行为与现状完全一致（现状测试全绿即证）；□ hisbase 开启时 fixture 链路映射正确；□ join 命中率数字进交付报告；□ check_isolation 过。

### P-C 配置模板与文档同步

1. `config.example.json`：五源（病理/气管镜/血透/电测听/hisbase）节点齐备（enabled=false、占位 DSN）；`service.anchor_mode` 注释补充"生产过渡建议=paperless_rpa（用户已拍板，时效≈出院后5天；179 作废注记）"；
2. prearchive `README.md`：§衔接表更新（四源 BLOCKED→已实测回填、hisbase 工号映射通道就绪、心电=唯一未接源待用户提供）；§已知限制同步；
3. `docs/ACTIVE/031` 衔接表/`033` 状态如需微调一并做（勿改 031 已执行记录正文，只加补记）；
4. `.gitignore` 追加 `docs/docs/`。

**验收断言**：□ config 校验测试过；□ README 无过时描述；□ .gitignore 含 docs/docs/。

### P-D 门禁、登记与提交

1. 全绿门禁：主服务 pytest 1176+ 全绿、prearchive 196+新增全绿、check_isolation、compileall、命名检查；
2. 提交拆分（建议 4 笔）：`feat(prearchive): 四源真实采集器(病理/气管镜/血透/电测听,实测列回填)`、`feat(prearchive): HIS基本信息工号映射与科室规范化(hisbase,T8-3)`、`chore(prearchive): 配置模板与README同步五源现状`、`docs: 033办结注记与INDEX登记(2026-08-29会话遗留收口)`（T0 的 3 个脏文件并入最后一笔 docs）；
3. 01 记录登记（含 hash/测试数字），行尾 `[T8-2b/T8-3 done]`；交付报告（完成清单/断言勾选/数字/hash/遗留=心电+W10+现场项）。

## 4. 红线（违反=任务失败）

1. **零生产接触**：开发与测试一律 fixture；禁止从开发机连任何真实库；sjzc 仅限 §3-P-B 第 4 点的只读聚合采样与词表校准（患者明细一行不许落盘）；
2. `prearchive_service` 禁 `import app.*`（check_isolation 保持过）；血透 `IDNo`/任何 PHI 不进仓库；
3. 禁改测试断言凑绿；禁动 14 条已授权正式规则与 `system_push_rules.json`（等 W10）；
4. 每包独立提交、不 push；真实 DSN/口令一律不进仓库（生产凭据走平台受控配置）；
5. 完成判定=P-D 门禁全过+登记齐，缺一即未完成。

## 5. 交付后给用户的提醒

剩余外部依赖仅两项：①W10 推送类规则清单（病理+PACS 两条起步，5 字段模板见 033 请求三）；②心电对接信息（库型/实例/表名，用户后期提供）。
