# PROMPT — 041 v1.1 规则中心隔离环境可用性一次性执行

交接日期：2026-09-04 ｜ 委托：用户 ｜ 起草：Grok 4.6 ｜ 作业书版本：**041 v1.1**
你的身份：一次性执行 AI。按本提示词 + **`docs/ACTIVE/041` v1.1（唯一作业书）** 一口气做完。

## 0. 必读顺序

`AGENTS.md` → `开发起步包/README.md` → `00_AI协作规则.md` → `01_统一修改记录.md` 末 10 行 → `docs/INDEX.md` → **`docs/ACTIVE/041` 全文（认 v1.1 修订记录，不要按记忆执行 v1.0）** → 本提示词其余部分。

不要从 `docs/archive/` 找执行依据。不要并行执行 023/004C/036-RP7/017 WP6。

## 1. 基线（2026-09-04 定格）

- 分支 `fix/ora-12609-p4-error-code`。037+039 **整包仍在工作区未 commit**——保留，禁止还原，禁止自行 commit/push。
- 门禁锚：主服务 1272 passed；prearchive 270 passed + 1 skip；unit 54；e2e 52+17skip 0 fail。
- 生产 `med-audit:latest=0ff639fb78e0`，规则中心框架在、**全开关关、预检进程未起、BFF=503**。本轮 **零生产访问**。

## 2. 本轮一句话

隔离 demo 把归档前规则中心点到能用。v1.1 相对 v1.0 的硬差别：**T3 是权限双修**（读端点收紧 + `current_user_has_permission` 真实现 + BFF 签名必须 `get_user_permissions` 查库，禁止 `getattr(User, "permissions")`）；T4 角色只用 seed 实名 admin/auditor/dept_manager/clinician；T8 只更新状态不重写 INDEX。

## 3. 已裁定（禁止再争论）

1. 不重做 037/039，不启生产预检，不切 UI 默认入口，不写 004C，**不改 `app/database.py`**。
2. `User` 没有 `permissions` 属性。不修签名来源，sidecar 对 admin 也 403，T5/T6 会卡死——这是计划内必修，不是现场发挥。
3. 9 个读端点现状即无 `require_permission`，必须全部改为 `PERM_VIEW`。这是契约收紧，写进 042/101。
4. 只认 `DEMO_MODE` 注入 BFF；`PREARCHIVE_ADMIN_ENABLED` **键已在 `os.environ` 则不注入**。不要用 `TEST_ISOLATED_MODE` 做或条件。
5. T4 矩阵已锁死（auditor 仅 view；dept_manager/clinician 全无）。只改 `seed.py`，给 auditor 追加 `prearchive_rule_view`。
6. `ALLOWED_TARGETS` = **20** 条，不新增。
7. T7 **import** `app.auth._resolve_runtime_environment`，禁止抄第二套。
8. `--check` 必须带 Admin-Token + Actor 四件套 + HMAC；只带 token 会 401。
9. 用户未批准不 commit、不 push、不部署。
10. 不改 14 条正式规则 JSON；delivery/insurance/opendrg 保持关。

## 4. 执行顺序（041 §4）

T0 → T1 sidecar/HMAC check/编排 → T2 DEMO_MODE BFF → T3 权限双修 → T4 矩阵 → T5 生命周期 HTTP → T6 E2E（正向 failedApi 不含 `/api/prearchive-admin/`；降级豁免该前缀 502/503）→ T7 → T8 状态更新 → T9 门禁 + `docs/ACTIVE/042`。

每包测该包新测试，红则停。端口 **18600**、文件清单、矩阵以 **041 v1.1 正文为准**。

## 5. 门禁（仓库根）

```
python -m compileall app tests scripts prearchive_service
python -m pytest
python scripts/check_naming_convention.py
python -m pytest prearchive_service/tests -q
python prearchive_service/check_isolation.py
npm --prefix frontend run typecheck
npm --prefix frontend run test:unit
npm --prefix frontend run build
npm --prefix frontend run test:e2e
python scripts/run_prearchive_demo_sidecar_20260904.py --check
```

0 failed；计数不低于 §1 锚；oneshot uinext 默认 skip；isolation 零 `import app.*`。`--check`：端口占用则只探测；退出杀进程树。

## 6. 升级出口（停下问用户）

- 必须改 Dify/推送/调度/Relay 才能过测试。
- 必须改正式规则 JSON、生产配置、或 `database.py`。
- 必须 SSH/部署/开生产预检。
- 门禁连续两轮红且根因不在 041。
- 不要为角色名再问；矩阵已锁。不要擅自加 BFF 白名单路径。

## 7. 完成定义

041 v1.1 §12 勾选全过 + `docs/ACTIVE/042`（含两步启动命令与读端点契约变化）+ INDEX 状态更新 + 01 登记。未批准不 commit。
