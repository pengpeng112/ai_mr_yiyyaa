# PROMPT — 035 系统修复与测试一次性执行（交接提示词）

交接日期：2026-08-31 ｜ 委托：用户 ｜ 起草：ZCode(GLM-5.3)
你的身份：一次性修复执行 AI。按本提示词 + `docs/ACTIVE/035`（唯一作业书）一口气执行完成。

## 0. 必读顺序（严格照此读，勿跳）

AGENTS.md → 开发起步包/README.md → 00_AI协作规则.md → 01_统一修改记录.md 末 10 行 → docs/INDEX.md → **docs/ACTIVE/035 全文（作业书）** → 本提示词其余部分按需对照。

## 1. 当前基线（2026-08-31 定格，勿重复发现）

- 分支 `fix/ora-12609-p4-error-code`，HEAD=`fba8090`（W1 已裁定保留补登记并提交）；
- 基线数字：**主服务 pytest 1176 passed/0 failed；prearchive 212 项（211 passed+1 skipped）；ui-next Playwright 46 passed/11 expected skipped**；
- 工作区干净（除 review/ 与 docs/attachments/ 等忽略项）；
- 生产=10.10.8.84 容器 med-audit（镜像 latest=bb7104f4f7e0，回滚 tag rollback-pre-hotfix-20260830）。

## 2. 两个已裁决的关键事实（防你重蹈起草 AI 的覆辙，勿再按旧文档推翻代码）

1. **discharge_final 已有通用 fallback**（`app/services/scheduler_run_modes.py:187-205`：全 source 在 {dept_filter} 后追加出院日期过滤，无占位符时按 date_dimension=discharge_date 加载）。**AGENTS.md"仅 3/6 类型"的说法是文档漂移**——你的工作是 RP2=验证 fallback 对 admission_vs_first_progress/surgery_chain/discharge_vs_frontpage 真实 SQL 的有效性（注意过滤硬编码别名 `a."出院日期"`，SQL 无该别名即静默失效）+ 加告警 + 同步 AGENTS/INDEX 文档，不是重写转换。
2. **CSP 中间件已存在**（`app/security_middleware.py`：`script-src 'self' 'unsafe-eval'` +nosniff+Referrer-Policy，单测在 tests/test_cookie_csrf_security.py:144）。unsafe-eval 是 legacy Vue 运行时模板编译的必要项，**收紧会直接崩页面**——RP4 只做现状回归固化，不动策略。

## 3. 执行主线（035 §3/§6 为准，此处为顺序摘要）

**RP0 已完成**（解释甲+W1 保留，跳过）→ **RP8**（C1 relay 密钥：先生产只读核验实际值，仓库侧 `git rm --cached config/config.json`；生产换密钥须用户当次授权）→ **RP9**（C2 修复 nursing field_mapping `??ID/??`→对照真实列名；C3 补 syssvsscbc 类型级 api_key_enc，凭据走受控配置不落仓库）→ **RP1**（B2 双调度：先生产只读建同患者同日双结果/双告警时间线→本地 fixture 复现→按证据四选一策略：允许双发/终末覆盖抑制 daily/延迟 daily/仅抑制相同版本。**禁止盲目当日去重**——终末结果业务身份不同，tests/test_historical_rerun_replacement.py:46-48 有身份断言）→ **RP2**（B1 验证+告警+文档同步）→ **RP3**（B5 指标元数据块 generated_at/date_from/date_to/timezone/filters/semantics，导出保持 record_export_audit）→ **RP4**（CSP 现状固化用例）→ **RP5**（requests `==2.32.4` 精确 pin+requirements.linux/windows.txt+packages/ 离线轮子+镜像重建链；cx_Oracle/passlib 只登记风险）→ **RP6**（legacy E2E：独立 playwright 配置 base URL=/，含登录态与 mock /api/audit-types/prearchive；E2E 前必须 `npm --prefix frontend run build`；ui-next 基线不回退+新增≥2）→ **RP7**（预检上线 runbook，纯命令清单，跨天影子不阻塞主线）→ **全绿门禁**。

每包完成：写 checkpoint 到 `review/exec-log.md`（完成态/测试数/未决项），中断可续。

## 4. 全绿门禁（终版命令，仓库根执行；命令路径已修正勿再用错误写法）

```
python -m compileall app tests scripts
python -m pytest                                        # 0 failed，无意外 skip
python scripts/check_naming_convention.py
python -m pytest prearchive_service/tests -q
python prearchive_service/check_isolation.py
npm --prefix frontend run typecheck && npm --prefix frontend run test:unit
npm --prefix frontend run build
npm --prefix frontend run test:e2e
```

主判据=0 failed+无意外 skip；计数（1176/212/46）仅报告对比。任何一步红即停该包，不带病进下一包。

## 5. 回归红线（AGENTS 已登记，改到相关面时逐项自检）

serial/bulk 推送语义一致；单条事务失败隔离；daily/discharge 双 job 独立锁；Oracle 空串=NULL；pushed_flag/reviewed_flag/manual_override/skip_reason 保留；/api/logs NULL 容忍；新导出必调 record_export_audit；Dify mr_text→mr_txt 仅 pusher 映射；relay 配置部分保存不得整段覆盖；Vastbase 列名 .lower()；prearchive 禁 import app.*；血透 IDNo PHI 双保险；不动 14 条已授权规则与 system_push_rules.json。

## 6. 纪律与升级点

- **零生产写入**：RP8/RP9/RP1 的生产核验一律只读 SELECT（容器内 python 或平台受控连接器）；任何生产写入（换密钥/部署）停下向用户要当次授权+备份+回滚点；
- 每包独立提交（`类型(范围): 中文摘要`），本地 commit、**不 push 不部署**（用户未授权）；测试断言禁改凑绿；
- 每笔改动登记 01_统一修改记录.md（含测试数字与 hash）；docs/**.md 变动同步 INDEX；
- 升级用户的出口：①RP1 实测后双发策略二选一以上需拍板 ②RP8 生产密钥处置 ③RP9 需要生产侧凭据值 ④门禁连续两轮红。
- 完成定义：RP8-RP9-RP1-RP2-RP3-RP4-RP5-RP6 全部验收勾选+§4 门禁全绿+01/INDEX 登记齐+交付报告（含每包 hash/测试数/未决项）写入 docs/ACTIVE/036。

## 7. 交接快照

- 035 问题清单 17 条：C1-C3（配置安全）/B1-B5（后端）/F1-F3（前端）/P1-P2（预检）/W1（已闭合）；每条处置去向见 035 §3；
- 035 §4 有 RP1/RP2/RP3/RP6 逐用例表，§5 有回归矩阵，§8 有不修复项登记（勿自行扩大范围）；
- review/round-4/ 是计划的多审查证据（不入库，可读勿改）；
- 心电/W9/W10/四角色现场验收=外部依赖，只登记不等待。
