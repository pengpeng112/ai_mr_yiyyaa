# 交给执行 AI 的提示词

请在当前仓库执行 `docs/ACTIVE/050_DJ1_FIX_AND_BATCHED_COMMIT_ONESHOT_PLAN_20260921.md` **v1.1**（multi-review round-6 三方互查修订版），完成 T0–T5 六个任务包，并将结果写入预留的 `docs/ACTIVE/051_050_EXECUTION_DELIVERY_REPORT_20260921.md`。

先按 AGENTS 启动顺序读取文件（AGENTS.md → 开发起步包/README.md → 开发起步包/00_AI协作规则.md → 开发起步包/01_统一修改记录.md 末 10 行 → docs/INDEX.md），使用 med-audit-oneshot-delivery、med-audit-gates 两个 Skill；T3 涉及 demo 栈编排时使用 med-audit-demo-stack Skill。依据当前工作树和 01 记录执行，不按 HEAD 覆盖现场。

本执行请求允许：050 §2 清单 A 文件白名单内的本地修复（D-J1 A 案物化前移+失败降级 partial）、相关测试（spy 复现/契约 keys/故障注入）、隔离 L1 双栈起停（子进程显式 APP_DB_TYPE=sqlite）、demo 栈起停与全量门禁、锚更新（仅经脚本）、本地文档交付登记；**以及按 050 §3 T1 归属表分批（B1–B5）执行本地 git commit——**用户已于 2026-09-21 明示批准本提交条款（multi-review round-6 人工终审选项 A：五批本地 commit、不 push），授权已生效并登记 01 台账**（逐文件显式路径 add+逐批核对 staged；050 §0.1-7 与 §0.2 红线全数适用；本条款范围=本地 commit，不含 push）。已裁定的实现选择无需重新论证；先复现再修；遇到失败先定位，白名单内修复并重跑，继续不依赖失败结果的工作。

本请求不授权：git push / tag / PR / rebase / 分支切换与合并（commit 止于本地，且以用户明示批准为前提）、生产访问与写入、真实患者处理、真实 Dify/Relay/外部模型调用、临床规则语义变更、默认入口切换、删除或提交 `.v2c/` 与 `.video_agent/`。外部待验收事项按 049 §10 单列，不等待这些资料完成无依赖的本地工作。不清理或丢弃混合工作区，不终止未知进程（4173 只报不杀）。

若上下文或工具额度耗尽，在滚动 051 写准确剩余包、失败命令、证据与续跑点（每包完成即写 checkpoint 到 review/exec-log.md）；后续从该点继续，不重新生成计划、不假报完成。最终回复给出：完成项、各批 commit hash（或演练模式说明）、测试证据（定向+全量门禁+L1 期望集比对）、报告路径、扣留与偏差清单、生产/Git 状态。
