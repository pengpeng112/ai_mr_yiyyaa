# 执行提示词 — 046 v1.2统一修复与无纸化/JHEMR闭环

请在 `F:\python\前后端代码\ai_mrzk` 按以下要求直接开发、验证并交付，不只做评审或另写计划。

## 1. 唯一作业书和启动

1. 按AGENTS.md→开发起步包/README.md→00_AI协作规则.md→01_统一修改记录.md末10行→docs/INDEX.md恢复现场。
2. **完整读取** `docs/ACTIVE/046_PAPERLESS_RULES_JHEMR_CLOSED_LOOP_PLAN_20260910.md` v1.2（如已被用户进一步修订，以最新版为准）。使用 `med-audit-oneshot-delivery`；回归走 `med-audit-gates`；demo起停按 `med-audit-demo-stack`。
3. 045全部F0–F5/D1–D8和09-09测试问题已经并入046，按046 §2.1/§6.4执行，不启动平行045任务。045/044/09-09原报告保留作历史证据，旧045文件白名单和固定HEAD不覆盖本次新范围。
4. 读取 `review/exec-log.md` 最新 `[046:*]` checkpoint及047现有阶段记录；核对当前HEAD、status、相关文件hash，**从第一个未闭合子门继续**。没有checkpoint才首次T0；不要因换会话重做已完成包。

## 2. 用户目标与已确定事项

- 保留原有AI质控、Dify输入输出、严重度门禁、双调度、日志/反馈/Relay/H5。
- 无纸化92条逐条登记，AI自动匹配→候选规则→批量trial→用户观察/确认缺陷或误报→确认发布；不能自动判断的写明数据/语义缺口。
- JHEMR在“提交病历”动作中调用检查并提醒，不强制阻断、不自动扣分；可看缺陷、定位文书、整改和复检。**JHEMR客户端由用户另行安排开发，本任务必须完成Med-Audit服务端接口和联调包**，不能以客户端没完成为由只交空壳。
- ST-001修Oracle统计；ST-002修预览服务身份检查及自有端口；ST-003用可控时钟修时序。
- ST-004两条sidecar测试已接纳为当前工作基线：记录hash和验证，保留原作者未知，补登记，不再问是谁写的；其他未裁定改动保留并按AGENTS核对。
- 4173只报不杀，不按旧PID27276终止任何进程；不合适则自起空闲预览端口，只清理本任务所建进程。
- ST-005保留调度配置空body=200且零改动，不改422，增加兼容测试；OBS项按046 §2.1保留契约并补验证。

## 3. 严格执行顺序及验收

`T0 → T9a → T1a → T2 → T3 → T1b → T4 → T5 → T6 → T7 → T8 → T9b → T10`

- T1a只关闭匹配服务、stub、候选和申请契约的门；真实trial依赖T2/T3，属于T1b；页面验收属于T5。不得把后置验收没开始算作T1a失败。
- T9a先修基础工具/Oracle/测试兼容并做聚焦+quick；T9b再做新链路运行诊断。
- F05旧测试断言在T2明确迁移，保留负例场景并登记047变更表。缺文书不能盲目全判fail：已确认必需+源完整+过期限才是缺陷，未知/不适用/pending按契约区分。
- 白名单在第一次增加内部端点的同包同步更新：把固定20测试改为获准方法/路径集合的精确比较及权限矩阵，不能删除防护或只改成数量下限；新增内部路径不一定等于外部5接口。
- 新增权限和DDL走显式迁移脚本，禁止挂init_db/_ensure_default_rbac_permissions/启动或访问钩子自动写入；fixture/demo可显式隔离seed。
- T7 GET schema必须完整实现046 T7.1的汇总字段/计数单位/覆盖率分母/queued未定计数null；不能只返回issues。
- 运行链路测试须从合成触发正常入口开始，自动持久化、自动入队、Mock接收、ACK、整改、复检、新版本解决旧问题。**手工插Outbox或直接调用enqueue不能作为运行接线完成证据。**

每个子包完成立即跑有意义的专项测试并追加checkpoint。失败先在授权范围内定位修复，暂停依赖包但可推进无关项；不要每包停下等“继续”。例外仅限真实外部资料、权限或业务口径确实缺失且影响当前动作，说明具体缺什么；已明确裁定的事项不得重复问。

## 4. 一次完成优先；工作量确需两次时

默认持续完成全部任务。若确需分两次，仅在T5已验收后交接：第一段=T0/T9a/T1a/T2/T3/T1b/T4/T5；第二段=T6/T7/T8/T9b/T10。第一段实际交接前跑当时完整门禁与专项E2E，保留进程/文件/测试证据，在047写“第一段完成，总体未完成”，第二段从checkpoint继续。同一047更新，不新建另一份执行计划或048报告。

上下文压缩不算需要暂停；自动续跑时直接恢复checkpoint，不重复生成规则或重跑所有完成包。

## 5. 交付及授权范围

- 总交付：`docs/ACTIVE/047_046_EXECUTION_DELIVERY_REPORT_20260910.md`，含各包结果、92项覆盖、修复证据、契约/断言变更、模型stub与真实运行区别、JHEMR能力级别、启动方法、迁移/回滚和外部未完成项。
- 045的义务不能丢：044仅追加§11，完整D1–D8更正，保留原§0–§10；补记09-08未署名AI指令修订、ST-004本次接纳、合并执行结果。日期按实际补记日，不伪造历史留档/作者/测试。
- 完成 `python scripts/run_gates_20260906.py --full --anchor-file docs/reference/gate_anchors.json` 和本计划新增专项/两套前端实际API E2E；必测项SKIPPED不能算完成；数量据实际collection记录，不下调锚。
- 更新INDEX、023仅阶段行、启动包、01；045只在其全部验收完成后标“已执行（合并046完成）”。
- 本提示词授权本地开发、合成环境试验和验证。真实患者回测、生产迁移/部署/外发、JHEMR客户端修改不在本地执行授权内；后续已有具体授权可核对后继续，不反复索取。默认不创建cron、不修改门禁锚、不commit/tag/push/PR；旧其他任务Git授权不扩展到本次新增内容。
- 真实JHEMR联调和生产试点尚未完成时如实报告，不把本地Mock成功写成真实对接。缺外部资料不妨碍交付本地完整功能、明确的配置入口和联调包。
