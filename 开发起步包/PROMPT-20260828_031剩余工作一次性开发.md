# PROMPT — 031 剩余工作一次性开发（v2，对应 031 v4.3；multi-review round-3 四方复核版）

> 交接日期：2026-08-29（v1 2026-08-28） ｜ 委托：用户 ｜ 起草：ZCode(GLM-5.3)，经 multi-review round-3 四方复核（GLM+Kimi+Grok+Codex 全在场，15 项裁决落实于 031 v4.2；v4.3 含用户 2026-08-28 事实纠正与质控七问拍板）
> 你的身份：一次性开发执行 AI。**按本提示词直接开工，按任务包推进，一口气完成；支持跨会话断点续跑。**

## 0. 唯一权威规格（先读完再动手）

**`docs/ACTIVE/031_REMAINING_WORK_ONE_SHOT_LOCAL_EXECUTION_PLAN_20260828.md`（v4.3，含附录 A 快照数据）**——唯一施工图，本提示词只引路，冲突以 031 为准，有异议登记不擅改（031 §0.4）。

必读顺序（031 §0.1）：`AGENTS.md` → `开发起步包/README.md` → `00_AI协作规则.md` → `01_统一修改记录.md` 末 10 行 → `docs/INDEX.md` → **031 全文（含 §9-§12 用户业务需求+附录 A）** → 按包引用 001（仅历史证据，禁拣过时待办）、023 §0.6/§3/§9.1、026 §4/§5、029（含文头 2026-08-28 事实更正）、030 v1.1。

## 1. 关键事实（避免踩坑，全部已实证）

- **179 不是数据库**（嘉和应用服务器）；电子病历库唯一=177:5432；完成事件不落库——触发过渡锚点=无纸化 RPA 表（用户已拍板，≈出院后 5 天），终态候选=无纸化接收侧到达时间戳（W9 核证）。
- 主服务基线**3 个红测试**（patient_qc 导出空结果语义）待修；prearchive 105 项全绿需保持。
- `jhmr_file_index` 在 177（52 列，`topic` 标题含时间戳）=术后首程 24h 判定时间源（用户拍板口径）。
- `pat_visit.alergy_drugs` 填充率仅 8.1%——首页空项族不做（勿被字段存在迷惑）。
- 92 条快照数据已固化在 **031 附录 A**（T2-7 fixture 直接转换，勿凭空造字段）。
- RPA 表 `COMPLETED` 恒 1 无区分度——**增量信号=UPDATEAT**；该模式下 require_source_ready 门必须禁用（否则规则全 skip）。

## 2. 任务：按 031 顺序执行 T0→T8（T8 在 T2-7 后、T3 前）

| 包 | 一句话内容 |
|---|---|
| **T0** | 基线+9 个未入库文件分两笔提交（fix(prearchive) 4 代码文件 / docs(verify) 含 029/030/**031**+INDEX+01记录） |
| **T1-0** | 修 3 个红测试——**主路径已预授权**：按测试契约恢复"空命中=成功导出 0 计数+记审计"，detail 改英文；MOCK 回退只是辅核；仅当 blame 证明 400 是正式业务决策才升级用户 |
| T1-1~8 | 主服务收尾（留存只核对禁重写/inpatient_date/告警导出+后台列表测试/健康核对/**Cookie-CSP 降级须升级用户**/UI Next 静态降级/文档治理/metrics 骨架） |
| T2-1~6 | anchor_mode 三模式（默认 finished 不变）/doctor_guid 档（BLWS_SQL 补两列）/Oracle store mock/4 条实测词表规则/**术后首程时间源=jhmr_file_index.topic 解析（fetch_file_index+标题时间解析+doc_time_source 配置）**/DbStateStore 独立 metadata/README |
| **T2-7** | 无纸化对接：CDMS 第五源（T_MARK_ITEM 分页拉取+T_MARK_MAIN/DETAIL 仅聚合，meta SQL 禁 FPATIENTID/FBIHID/FBINCU/FNAME）+同步工具（五差异只报告）+回测框架（FID 取值域=附录 A；CDMS 非文书源禁接 builder） |
| **T8-1** | RPA 触发：增量=UPDATEAT；检查键四元组含 FBINCU；分页稳定次键；身份适配器独立（标注待 W9 核）；**该模式水位门禁用+回归测试**；RPTCOUNT 仅总量对账不宣称护理缺项；README 写时效+语义差距声明 |
| **T8-2** | 新七源采集器：七标签勿漏 xd_itf；PACS=mysql 入校验（本地免驱动）；接线面六处必改+可实测源 e2e 进 documents；病理/心电（对接信息用户后期提供）骨架实质断言 |
| T8-3 | 基本信息视图（VW_user_info 工号映射占位），默认 disabled |
| **T8-4** | 推送规则通道：先扩 DSL（TRIGGER_KINDS+report_expected、KNOWN_SOURCE_LABELS 扩七源）再建 system_push_rules.json 多文件合并；不写真实规则（等用户 W10） |
| T3/T4/T5/T6 | C# 助手合并弹窗/治理（.gitignore 补 review/、AGENTS.md 补约定、SQLite 备份演练、依赖审计只报告）/全绿门禁/登记提交交付 |

每包精确步骤、逐条验收断言、红线与降级出口都在 031 §3/§12 后正文——执行到该包时逐字对照。

## 3. 红线（违反=任务失败）

1. **零生产接触**：不连任何真实库（177/CDMS/各 T_ITF 源）/不 SSH/不 docker/不 DDL/不发真实推送；真实网关惰性建连+DSN 占位+默认 disabled；
2. 不改主服务推送主链路（AGENTS 回归敏感面全继承）；`prearchive_service` 禁 `import app.*`（check_isolation 保持过）；
3. 禁改测试断言凑绿；T5 门禁失败先判"基线既有 or 本轮引入"；
4. PHI 不落仓库（fixture 虚构 TEST 前缀；患者明细一行不出库）；
5. 签字硬序两轨：t_mark_item 规则等质控科签字（030 v1.1 已拍板七问，正式签字+版本号 `YYYY.MM.DD-qc-vN` 到位前 mark_item_fid 保持 null）；系统推送类规则等用户 W10 清单——都不写具体规则；
6. 每包独立提交（01 记录随包豁免；config.py 被 T2-7/T8-2 先后改属预期）；禁止 push；
7. 每包完成：验收断言勾选→01 记录+`[031:Txx done]`→按 031 §5 提交→下一包；中断重启从 01 记录尾行+git log 续跑。

## 4. 验收与收尾

全绿门禁（031 §3-T5）：主服务全量 pytest（T1-0 后 0 fail）+prearchive 全量（105+新增）+check_isolation+compileall+命名检查+前端静态检查。完成后：01 记录终行 `[031:ALL done]`、INDEX 同步、交付报告（完成清单/测试数字/commit 清单/未做=W1-W10 复述/证据附录/异议登记）。给用户的三件事提醒：①质控科正式签 030 v1.1；②信息科：病理驱动+四源登记；③用户自己的 W10 推送规则清单（病理+PACS 先给两条即可）。
