# PROMPT — 20260908 044 核查修复一次性执行

> 交接日期：2026-09-08 ｜ 委托：用户（「核查 044，并生成计划让别的 AI 修复」）｜ 起草：Grok 4.6 ｜ 作业书版本：**045 v1.0**
> 你的身份：一次性执行 AI。按本提示词 + **`docs/ACTIVE/045` v1.0（唯一作业书）** 一口气做完。交付=044 文末 §11，**不新建 046**。

## 0. 必读顺序

`AGENTS.md` → `开发起步包/README.md` → `开发起步包/00_AI协作规则.md` → `开发起步包/01_统一修改记录.md` 末 10 行 → `docs/INDEX.md` → **`docs/ACTIVE/045` 全文（认 §0 / §1 已取证表，禁止当新发现重做）** → `docs/ACTIVE/044`（只读 §0–§10，准备追加 §11）→ 本提示词其余部分。

相关 skill：`med-audit-oneshot-delivery`、`med-audit-gates`、`med-audit-demo-stack`（仅当 §7 要起 demo 补双轮时）。

## 1. 基线（2026-09-08 定格）

- 分支 `fix/ora-12609-p4-error-code`；HEAD 应为 **`58ea350`**。
- 工作区是脏的，分四层，**全部保留，禁止 reset/clean/stash**：
  - A 043 起草：INDEX/README/01 早期 M；043；两份 PROMPT-20260906；export 脚本
  - B 043 执行：3 Skill、3 门禁脚本、gate_anchors.json、044、4 份新测试、conftest 守卫、C1–C5
  - C 09-08 指令修订（**不回滚**）：AGENTS 核心原则、00 §2/4/6/8、023 §9.1、history-remediation、Skill 正文、INDEX 文首；备份在 `review/instruction-revision-20260908/`
  - D 本作业书：045、本 PROMPT、INDEX/023/README/01 的 045 登记
- 权威门禁仍以 `review/gate-runs/20260907-215610/` 为准（主 1327 / prearchive 283+1 skip / unit 57 / e2e 52+17 skip / legacy 双轮 PASS）。
- 锚 json **保持首版**（1295/283+1/57/52+17），本轮不上抬。
- 零生产。

## 2. 本轮一句话

044 主体合格。修 `run_gates` 两处缺陷（PASS 耗时未累加、sidecar 拉起失败未杀树），044 文末追加核查更正，补登记 09-08 指令修订。不要重做 043，不要改写 044 原文数字。

## 3. 已裁定（禁止再争论）

1. 只做 045 §4 的 F0–F5。
2. **044 §0–§10 不改写**；更正只追加 §11。
3. 09-08 指令修订保留；01 补登记执行者写 **未署名AI**（依据见 045 §3.4）。
4. 不创建 cron、不跑 `update_gate_anchor`、不 commit（除非用户当场另批）。
5. 业务代码零改动。测试只增不减。
6. `--quick` 必须过；`--full` 无 demo 时 legacy=`SKIPPED(no-demo)` 必须在 §11 声明，禁止把 SKIPPED 写成双轮完成。
7. 停机：包红即停；作业书缺陷走升级出口，禁止现场改口径。

## 4. 执行顺序

F0 冻结 → F1 修脚本+追加单测 → F4 四文件 pytest（含新单测）→ F5 `--quick` 再 `--full` → F2 044 §11 + INDEX → F3 01 两行 + README 入口改为已执行。

每包绿再进下一包；checkpoint 写 `review/exec-log.md`（不入库）。F2/F3 放在门禁之后，避免先改文档后红。

## 5. 门禁（仓库根）

```
python scripts/run_gates_20260906.py --quick
python scripts/run_gates_20260906.py --full --anchor-file docs/reference/gate_anchors.json
```

判据见 045 §7。最终以脚本 Markdown 表为准，原样贴进 044 §11。

若需补 legacy 双轮：先读 `med-audit-demo-stack`，**只起 demo 主服务，不要预先起 sidecar**；跑完按 skill 停栈。4173 只报不杀。

## 6. 升级出口（停下问用户）

须改业务代码/生产；须回滚 09-08 修订；锚低于 1295 且根因在旧三包；本轮就要 cron/上抬锚；作业书缺陷致包红。

## 7. 完成定义

045 §10 勾选全过 + 044 文末 §11 + INDEX 023/044/045 状态 + 01 两行（09-08 补登记 + 045 执行）+ README 入口已执行。未批准不 commit。
> 2026-09-10更新：本提示词对应045已并入046 v1.2统一计划，尚未实施。请改用 `开发起步包/PROMPT-20260910_046统一修复与无纸化JHEMR闭环执行.md`；以下原提示词保留作历史依据，不再独立启动。
