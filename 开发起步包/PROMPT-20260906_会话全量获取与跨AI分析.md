# PROMPT — 20260906 会话全量获取与跨 AI 分析（交给执行 AI）

> 交接日期：2026-09-06 ｜ 委托：用户 ｜ 起草：ZCode/GLM-5.3（会话导出方）
> 你的身份：一次性分析 AI。按本提示词把 Med-Audit 项目下历次 ZCode 会话**取全、读懂、出分析报告**。
> 本提示词自包含：不需要你读过原始对话即可开工。

---

## 0. 环境事实（先确认，缺一样就停下报告用户）

- 机器：Windows 10，Git Bash 可用，`python` 可用（3.11+）。
- 项目仓库：`F:\python\前后端代码\ai_mrzk`（git 仓库，分支与 origin 同步，工作区干净）。
- ZCode 本地库：`C:\Users\Administrator\.zcode\cli\db\db.sqlite`（约 714MB，**必须只读打开**，见 §2-C）。
- 已有一份导出：`F:\python\前后端代码\ai_mrzk\review\conversations-export-20260906\`（13 个主会话 Markdown + INDEX.md，约 1.9MB，时间跨度 2026-07-07 ~ 2026-09-06）。
- 导出工具：`F:\python\前后端代码\ai_mrzk\scripts\export_zcode_conversations_20260906.py`。

## 1. 任务一句话

获取该项目下**全部**历史会话（含可选的子代理会话），结合仓库权威台账，产出一份**跨会话工作脉络分析报告**：做了什么、决定了什么、哪些没做完、有什么风险和矛盾。

## 2. 数据获取（按 A→B→C 逐档执行，A 成功即可进入分析；要"全量"就做到 B）

### A. 读现成导出（最快，已验证可用）

```
cd "F:\python\前后端代码\ai_mrzk"
ls review/conversations-export-20260906/
```

- 先读 `INDEX.md`（清单+阅读顺序），再按 `01_…md` ~ `13_…md` 时间序读。
- 每份格式：`# 标题` + 会话元信息 + `## [n] 👤用户/🤖助手` 正文 + `> 🔧 工具(参数) → 状态` 摘要行。
- 该导出**不含**子代理会话、**不含**工具输出原文、**未打码**——见 §6 红线。

### B. 重新导出全量版（含 27 个子代理会话 + 工具输出截取 + 敏感串打码）

```
cd "F:\python\前后端代码\ai_mrzk"
python scripts/export_zcode_conversations_20260906.py \
  --include-subagents --include-tools --tool-output-chars 800 --redact \
  --out review/conversations-export-20260906-full
```

- 预期 40 个会话文件；若报"找不到 ZCode 库"，回到 §0 核对路径。
- 导出脚本本身是只读的（`file:...?mode=ro`），可放心重复执行。

### C. 直连原始库兜底（仅当 A、B 都失败；只读！）

```python
import sqlite3
con = sqlite3.connect(r"file:C:\Users\Administrator\.zcode\cli\db\db.sqlite?mode=ro", uri=True, timeout=10)
# 三张表：session(directory=项目路径, title, parent_id) → message(session_id, data含role, sequence)
#         → part(message_id, data含type=text/tool/reasoning, sequence)
rows = con.execute("SELECT id, title, time_created FROM session WHERE directory=? ORDER BY time_created",
                   (r"F:\python\前后端代码\ai_mrzk",)).fetchall()
```

**禁止**去掉 `mode=ro`；禁止任何 INSERT/UPDATE/DELETE/DDL；禁止把该库文件复制出本机。

## 3. 必读的权威上下文（分析前按序读完，用于交叉验证）

1. `开发起步包/README.md` —— 目录地图与当前入口。
2. `开发起步包/01_统一修改记录.md` —— **跨 AI 权威台账（append-only）**。以它为准绳：对话是过程，台账是结论。重点读 2026-08-24 之后的行。
3. `docs/INDEX.md` —— 文档索引与"已核验的功能状态"表。
4. `AGENTS.md` —— 仓库红线（你也要遵守其中与本任务相关的部分）。

## 4. 分析维度（报告必须覆盖这六项）

1. **时间线脉络**：13 个主会话各做了什么（一句话/会话），串成项目演进史（7 月文档与 Dify 提示词 → 8 月预检一期/031 → 9 月 035 修复/oneshot 测试/039 规则中心/041 隔离可用性）。
2. **关键决策与拍板**：用户在对话中明确拍板过什么（如：允许双发、anchor_mode=paperless_rpa 过渡、双人审批不强制、山东济南 DRG、角色矩阵锁死等），标注出处会话编号。
3. **未完成与待授权**：对照台账与对话，列出等待用户/院方/外部材料的事项（生产预检影子 7 天、EMR/HIS/医保接口、UI Next 切换、历史 high 整改、W10 清单、FID 回填等）。
4. **矛盾与偏差**：对话过程说法 vs 台账/文档最终说法不一致的地方（已知例：INDEX 旧文"discharge 3 个无转换类型"后被 2026-09-01 只读核验更正为文档漂移）。
5. **风险与隐患**：对话中出现但可能未闭环的问题（测试环境泄漏、E2E 口径豁免、生产乱码类事件等）。
6. **给用户的行动建议**：按优先级排 ≤5 条，每条注明"需要用户提供什么"。

## 5. 交付物与格式

- 写入：`F:\python\前后端代码\ai_mrzk\review\conversation-analysis-20260906\分析报告.md`（含附录：逐会话一句话索引表）。
- 报告结构：`TL;DR（≤10 行）→ 六个分析维度章节 → 逐会话索引表 → 证据引用（会话文件名+小节号）`。
- 结论必须带证据引用（哪个文件哪一节/台账哪一行），无证据的推测明确标 `[推测]`。
- 中文书写；技术名词保留英文。

## 6. 红线（违反任一条=本次分析作废）

1. **零生产访问**：不 SSH 10.10.8.84、不 docker、不碰生产 config/Oracle/Dify/Relay。
2. **零仓库修改**：只允许新建 `review/conversation-analysis-20260906/` 下的报告文件；不改任何代码/文档/配置；不 git commit/push。
3. **PHI 不外传**：对话若出现患者标识（住院号/姓名/身份证），报告中只引用"出现于 X 会话"，不复制原文；报告与导出都不得上传公网服务（打码版除外）。
4. **ZCode 库只读**（§2-C 已给只读姿势）。
5. 不臆造：找不到的证据就写"未找到"，不要编造会话内容。

## 7. 升级出口（停下问用户，不要自行改道）

- §0 环境缺项（库不存在/导出目录丢失/无 python）。
- 导出脚本运行报错且 10 分钟内无法修复。
- 会话数量与台账明显对不上（如缺 2026-08 月会话）。
- 用户要求你"顺手修复"对话里发现的问题——超出本任务范围，先出报告再议。

## 8. 完成定义

- [ ] 全量会话已获取（至少 A 档；建议 B 档）
- [ ] §3 四份权威上下文已读
- [ ] 报告覆盖 §4 六维度 + 逐会话索引 + 证据引用
- [ ] 报告落位 §5 路径，未违反 §6 任何一条
