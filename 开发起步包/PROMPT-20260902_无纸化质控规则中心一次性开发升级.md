# 一次性执行提示词：无纸化质控规则中心 + EMR/HIS JSON + 医保 DRG 插件

你是本项目的主架构师、后端开发、前端开发、测试负责人和部署负责人。请在单一连续会话中，按仓库现有规则尽可能一次性完成 039 计划；不要只出分析或再次生成计划，要实际开发、测试，并在满足生产门禁后执行已授权的安全升级。

## 一、必须先读

按以下顺序完整阅读，不得跳过：

1. `AGENTS.md`
2. `开发起步包/README.md`
3. `开发起步包/00_AI协作规则.md`
4. `开发起步包/01_统一修改记录.md` 末 10 行
5. `docs/INDEX.md`
6. `docs/ACTIVE/037_ONESHOT_FINDINGS_REPAIR_EXECUTION_PLAN_20260902.md`
7. `docs/ACTIVE/039_PAPERLESS_QC_RULE_CENTER_JSON_EMR_HIS_INSURANCE_ONE_SHOT_PLAN_20260902.md` 全文
8. 039 列出的 026/028/029/030/031/032/034/036、`docs/reference/101-103`、`docs/skills/med-audit-codex.md`、`prearchive_service/README.md`
9. 涉及其他系统字段前，读 `F:\python\数据资产\AGENTS.md`、其 `开发起步包/README.md`、`55_系统未完成事项统一执行计划.md`、`三仓库互通地图.md`

## 二、先处理前置冲突

- 先执行 `git status --short`，识别所有他人改动并保护它们。
- 如果 `docs/ACTIVE/038_*` 不存在，说明 037 尚未执行。先完整执行 037 并生成 038，再重新读取 039；037 与 039 禁止并行、禁止交叉覆盖。
- 如果发现上一任修改未登记，按 AGENTS.md 报告并处理，不得默认夹带。
- 不得用 `git reset --hard`、`git checkout --` 或删改他人文件。

## 三、用户已经拍板，不要重复询问

1. 规则发布不强制双人审批：实现为可配置，默认 `require_separate_approver=false`；同一管理员可编辑、审批、发布，但审计、不可变版本、SHA 和回滚必须保留。
2. 医保地域/类型：山东省济南市、DRG；具体年度、分组方案、权重后续在系统内维护。未配置年度规则包前，医保正式判定必须关闭。
3. 院方允许使用和集成 OpenDRG；但你仍须读取实际仓库 LICENSE/规则数据授权并生成 NOTICE。若许可证没有明确授予复制/修改/分发权，不得 vendoring，只实现隔离 adapter 并记录阻断。
4. 试点科室、规则范围、只提示/扣分/阻断、推送严重度全部做成配置。安全默认：只提示、不扣分、不阻断；初始科室为空时不得向临床触达。
5. 用户已允许 039 范围内的生产新增 DDL、代码/UI 部署和安全开关启用，但必须先备份、提供回滚点、全门禁通过并写后验证。
6. 本次没有授权 Git commit/push，没有授权业务源库写入，没有授权改 Dify/Relay 既有语义。

## 四、当前仍缺资料的项目

- EMR 接口 URL、认证、字段映射和 ACK；
- HIS 接口 URL、认证、字段映射和 ACK；
- 接收方是否需要患者姓名等超出最小集的字段；
- 山东济南 DRG 的具体年度和正式规则包。

缺这些资料不许停掉整个开发：完成通用接口、Mock、配置页和测试，真实 destination/医保判定保持 disabled，并在 040 中列为明确待办。不得猜接口、猜字段或猜医保版本。

## 五、必须完成的开发范围

严格执行 039 的 T0→T10：

- 在既有 `prearchive_service` 上增量建设，不重建服务；
- file/compare/registry 三模式，默认 file；
- 不可变规则版本、草稿、校验、审批、发布、回滚、审计和文件规则无损导入；
- 现有正式规则文件保持字节不变，fixture 下 file 与 registry 结果逐字段零差异；
- JSON Result/ACK/Rule DSL Schema；
- EMR/HIS 独立 destination、HMAC、SSRF 防护、Outbox、幂等、重试、lease、dead、reconciliation；
- 医保 plugin protocol、Noop、确定性编码插件、OpenDRG 隔离 adapter；
- 主服务白名单 BFF、fail-open、独立 actor 签名和六项 additive RBAC；
- Legacy 与 UI Next 两套“质控类型”页面都要具备规则维护、版本 diff、试运行、审批发布回滚、目标状态和 Outbox；
- 读取 `F:\python\数据资产` 的机器资产建立 confirmed/candidate/blocked 字段注册，不复制外仓大文件；只有必要时才按其规则走 sjzc 限量只读，阶段 A 不连接生产活库；
- 所有新能力默认关闭，禁止影响六类 Dify、调度、PushLog、Relay/H5、日志导出和当前预检结果。

## 六、禁止事项

- 不把规则引擎插入现有 `push_executor`；
- 不修改 `app/dify_pusher.py`、`app/services/push_executor.py`、`app/scheduler.py`、`app/services/relay_alert_service.py`；确实不可避免时停止并报告；
- 不修改 `config/config.json` 和三个正式规则文件来凑测试；
- 不允许规则执行任意 SQL/Python/JS/Jinja/eval/Shell/URL；
- 不将姓名、身份证、电话、地址、医保号、病历正文写进日志、fixture、报告或 Git；
- 不真实调用 Dify、Relay、EMR、HIS 或医保接口做测试；
- 不写 HIS/JHEMR/CDMS/LIS/PACS 等业务源数据库；
- 不因预检服务不可用而阻止主服务启动；
- 不复制 GPL OpenHIS 代码；不把 GitHub 可下载等同于许可证允许。

## 七、测试和生产升级

1. T0 先记录当前基线；037 若已执行，以更新后的数字为最低基线。
2. 每个包跑聚焦测试，最后完整执行 039 §12 的 prearchive、主服务、compileall、命名、UI typecheck/unit/build、默认 E2E 和 legacy 真后端 E2E。
3. 所有测试只用 fixture/Mock/127.0.0.1，真实网络调用次数必须为 0。
4. 全门禁通过后，按 039 B3 执行用户已授权的生产安全升级：
   - 精确核对生产目标路径、容器、应用库类型和当前镜像；
   - 备份配置、镜像和新增表相关 schema，记录可执行回滚命令；
   - DBA/受控连接只执行本计划新增 `MED_PREARCHIVE_*` DDL，不改业务源库；
   - 部署独立预检服务和主服务/UI；
   - 可以启用管理页面/BFF；运行规则保持 `file` 或经零差异校验后的 `compare`；
   - `result_delivery.enabled=false`；EMR/HIS destination=false；
   - `insurance_qc.enabled=false`，直到山东济南具体年度规则包到位；
   - 完整验证健康、六类 Dify 既有路径、双调度、日志、反馈、Relay、Legacy、UI Next；
   - 热更新必须持久化到新镜像，记录新镜像 ID 和回滚 tag。
5. 不要为了“一次性”跳过 7 天 compare 观察。单会话完成 B3 后，清楚报告“已升级待用户核查”；B4/B5/B6 中依赖观察期或缺失接口的动作保留待办。

## 八、交付

- 创建 `docs/ACTIVE/040_039_EXECUTION_DELIVERY_REPORT_YYYYMMDD.md`；
- 同步 `docs/INDEX.md`、`prearchive_service/README.md`、必要的部署说明和 `开发起步包/01_统一修改记录.md`；
- 不新增 docs 顶层散文件；
- 不经用户另外批准不 commit、不 push；
- 最终回复必须列明：完成项、未完成项、全部改动文件、测试命令与数字、生产备份/镜像/回滚点、DDL 实际对象、开关最终值、真实外发次数、OpenDRG 许可证结论、G2/G3/G4/G5 待办；
- 只有真实目标启用且对账完成，才能说“EMR/HIS/医保正式对接完成”。框架部署完成不得夸大为正式对接完成。

现在开始执行，不要再次输出新计划；遇到不确定项先按 039 的安全默认继续，只有会改变业务口径、需要缺失凭据、超出已授权生产范围或触碰禁止文件时才向用户提一个具体问题。

