# 048 — 系统功能、架构、前端与验收证据完善一次性执行作业书

> 版本：v1.0；制定日期：2026-09-15。
> 状态：已执行完毕，交付=`docs/ACTIVE/049_048_EXECUTION_DELIVERY_REPORT_20260915.md`（2026-09-15，T0–T7 全包；发现 D-J1 待授权项列 049 §10）。
> 从属：023 综合执行主线；承接 046/047 已完成的本地能力，不重做 046。
> 交付预留：`docs/ACTIVE/049_048_EXECUTION_DELIVERY_REPORT_20260915.md`（执行时创建，本次不创建空报告）。
> 目标：用一次集中执行完成有证据的本地缺口修复、工作台可用性完善、边界及故障测试和可复核交付；不把生产上线或临床有效性冒充为本地完成。

## 0. 为什么把每日免费执行用于这件事

每天选择一个能独立验收的主题，交给 AI 连续完成“取证→复现→修复→验证→交付”。本次主题是 **046 后的系统可靠性与工作台完整性**。已有功能较多，继续铺新模块的收益低于把既有能力做成可用、可复测的闭环。

本作业书是一个执行批次，T0–T7 为必做包；不是每天重复跑的全站审计。完成后，下次额度从 049 的剩余事项中选择一个主题，不重复扫描或重复造计划。外部依赖可优先预约，但不得占用本次执行的主路径。

### 0.1 修订记录与事实边界

| 版本 | 内容 | 证据性质 |
| --- | --- | --- |
| v1.0 | 取证、八个任务包、范围、验收和交接提示词 | 当前工作区静态读取 + 既有门禁原件；非 2026-09-15 全量测试结论 |

编制时 HEAD=`58ea350`，工作区有大量已登记但未提交的 043–047 与 09-12 指令修改。执行依据是当前工作树，不是单独 HEAD。保留已有改动，逐项核对登记；本计划不替未知改动认领归属。047 中“第二段不新建048”仅针对 046 第二段重复报告，本文件是新的完善专项。

### 0.2 已取证的问题与优先级

| ID | 优先级 | 当前证据（文件/定位词） | 本次应处理的结果 |
| --- | --- | --- | --- |
| F1 | P1 | `frontend/src/features/governance/WorkbenchPage.vue`：工具栏在 `v-if="!loadError"` 内 | 首次加载失败后仍可刷新恢复；详情和观察请求有独立失败处理 |
| F2 | P1 | 同文件 `canReview` 按 admin/auditor 角色名；`row.status`、`reason_code` 原样展示 | 按现有有效权限展示操作；状态中文化；不改后端权限分配 |
| F3 | P1 | `prearchive_service/prearchive/closed_loop_api.py::list_checks` 默认 limit=100、上限300，total=len(items)；Vue 无分页 | 可访问全部授权匹配结果；返回真实 total；排序稳定；两套界面接入 |
| A1 | P1 | 同函数查询全部正式 `IssueRow` 后在 Python 按就诊聚合 | 限定本页授权就诊集合并在数据库聚合；明确当前缺陷计数与历史 run 的关系 |
| Q1 | P1 | `scripts/run_gates_20260906.py::run_legacy_rule_center_e2e` 只指定 `rule-center.spec.ts` | 工作台专项纳入可复跑验收；必测项跳过不能报告本专项完成 |
| Q2 | P1 | `frontend/tests/e2e-legacy/workbench.spec.ts`：工作台允许空列表；正向 API 失败过滤整体排除 prearchive-admin | 加非空详情/整改链路与精确失败断言；不把接口异常误吞为成功 |
| Q3 | P2 | `gate_anchors.json` main=1295；047 §10 写锚1327；09-11 summary实际main=1376 | 区分机器锚、历史实际数、报告误写；不擅自上调/下调锚 |
| D1 | P1 | 047 §5：“代码回滚=整工作区未提交，直接丢弃即回第一段前状态” | 在新交付及047追加更正：只针对本次补丁回退，禁止丢弃混合工作区 |
| A2 | 待核验 | `match_service.py::run_task` 候选写入与进度更新分事务；`current is None` 分支 refresh(current) | 用故障注入核验重启重复候选/并发执行/消失任务；仅修复已复现问题 |

F1/F2/F3/A1/Q1/Q2 是源码可见缺口；影响程度仍由 T0/T2–T5 动态验证。A2 不是已证明的线上故障。目录中存在 TODO、stub 或旧报告“待做”本身不构成新增需求。

### 0.3 历史完成度基线

| 事项 | 编制时可证明的状态 | 本次处理 |
| --- | --- | --- |
| 045 修复 + 046 T0–T10 | 047、01 均记本地完成；代码存在 | T1 抽查需求→代码→测试→原始产物；不重做已成立项 |
| 09-11全量 | `review/gate-runs/20260911-090850/summary.md`：main1376、prearchive438+1skip、unit62、E2E52+17skip、legacy规则中心正向1/降级1 PASS | 历史证据，不能当今天测试；核对 raw/JSON 与专项覆盖 |
| 09-12指令治理 | 01登记；`review/gate-runs/20260912-153719/summary.md` quick4/4 PASS | 保留已批准自主性规则，不重新改写 Skill |
| JHEMR | 047：L1本地双服务mock客户端验收；L2/L3未开始 | 本地契约与失败路径回归；真实JHEMR接入由外部开发方完成 |
| 真实AI匹配、真实回测、W10清单、生产迁移 | 047 §5.2 明确未完成 | 形成具体输入/负责人/验收清单；不假造结果，不等待凭据才完成本地包 |
| UI Next、旧AI质控、双调度、Relay/H5 | 有历史代码与模拟回归；不等于现场上线验收 | T1/T5核验高风险交界，T6全量回归 |

## 1. 执行边界和默认决策

用户将配套 PROMPT 作为执行请求后，执行 AI 在下列本地范围连续完成，不逐包索要确认。本次计划编制仅修改文档，代码实现留给执行 AI。

- 只使用本地隔离数据库、TEST合成就诊、stub或回环mock模型/Relay；服务绑定127.0.0.1。生产服务器、真实患者库、本地真实配置不作为测试环境。
- 不执行生产DDL/DML、真实Dify/Relay/JHEMR调用、临床规则发布、历史整改、默认入口切换、Git commit/tag/push/PR。不得从023旧批准推导本任务授权。
- 保持legacy可用，不全面换UI框架；不引入微服务、Redis、Celery、新模型供应商或大版本依赖升级。
- 保持引擎五态、临床规则与阈值、人工状态不改引擎结论、患者ID+次数联合键、双模式锁、mr_text→mr_txt边界与隔离红线。
- 允许在任务包内修复复现缺陷、增加针对性测试、局部提取查询/错误处理逻辑和更新本次相关文档。权限展示使用既有能力，禁止凭角色名新增授权。
- 事实已成立且当前测试覆盖充分时记“已验证，无需修改”，不为凑数量重构。未知行为先只读取证/合成复现；业务语义不清且会改变结果时才暂停该项，继续其他包。
- 一次执行包含所有必做包；遇资源限制写精确checkpoint，同一049继续。不得把时间不足、外部事项未获批或一次失败包装成所有包完成。

## 2. 文件范围

### 2.1 允许读取

仓库源代码、测试、现役文档、脱敏门禁日志；不读取或输出秘密、患者附件、完整病历或生产配置密文。历史archive仅用于溯源。

### 2.2 允许修改（以任务关联为限）

- `frontend/src/features/governance/WorkbenchPage.vue`、相关API类型/端点、现有权限/错误展示工具及必要局部组件；legacy对应 `static/scripts/modules/prearchive_rule_center.js`、`static/templates/pages/audit_types.html`。
- `prearchive_service/prearchive/{closed_loop_api,issue_service,match_service}.py` 及其直接查询/事务辅助模块；主服务 `app/routers/prearchive_admin.py`、`app/services/prearchive_admin_client.py` 仅兼容透传/权限契约所需改动。
- 与上述行为及T5故障验证直接相关的 `tests/`、`prearchive_service/tests/`、`frontend/tests/`；隔离fixture和demo造数不得触及生产数据。
- `scripts/run_gates_20260906.py` 与现有相关测试；可新增 `scripts/*_20260915.py` 本地专项编排/证据工具；Playwright配置仅增加该专项运行能力。保持原有CLI兼容。
- 构建生成的 `static/ui-next/` 仅由既有同步工具生成，核查其diff，不手工清理他人文件。
- 本048执行状态、049报告、配套PROMPT、`docs/INDEX.md`、起步包README/01、023仅§0.6追加行、047仅追加更正节。

不改 AGENTS/00/Skill 授权规则，不改既有锚JSON，不改真实config/秘密文件，不修改临床规则JSON与工作流、不批量移动历史文档。不新增数据库结构；若T4实证需持久化唯一约束/迁移才能正确解决，提交具体方案，标该项受阻并继续其他包，不能以进程内锁声称跨进程可靠。

## 3. 执行顺序与任务包

依赖：T0→T1；T2→T3；T4可在T1后独立；T5在相关修复后；T6在T2–T5完成后；T7收尾。允许不相互依赖的检查同时运行，但共享端口、数据库和构建目录的测试必须串行，避免争用。无需要求执行工具一定支持多代理。

### T0 — 冻结当前现场和建立可复核基线

1. 按AGENTS启动顺序读文件，再读本048、046中相关验收条款、047、101、med-audit-codex及gates/demo Skill。
2. 在 `review/system-hardening-20260915/` 保存当前HEAD、git status、受影响文件SHA256、Python/Node/npm版本和本次修改前副本清单；执行前再次核对048/049编号和路径占用。
3. 对照01核验既有改动；已登记043–047及09-12内容可作为待测试基线。未知归属且确需修改的文件保留并报告具体冲突，继续无关包。
4. 核查127.0.0.1端口、PID及归属。按demo Skill创建本任务run-id；不可杀未知PID，4173只报不杀，按现有脚本选择自有备用预览端口。
5. 跑 `python scripts/run_gates_20260906.py --quick`，读取现有全量原始产物。基线失败先分类定位，不能通过跳测试或降低断言消除。

验收：现场清单、隔离环境证明、quick结果和基线问题表齐全；记录当前测试尚未覆盖的部分，不声称全量已绿。

### T1 — 功能、架构与历史交付追踪矩阵

在049建立一张矩阵，覆盖：认证/RBAC；六类AI质控与payload；serial/bulk/retry；双调度；日志/导出；反馈/Relay/H5；规则编辑发布/刷新；工作台/Issue；匹配/trial；Outbox；JHEMR；SQLite/Oracle迁移；legacy/UI Next。

每行必须有“入口/关键实现/现有测试node或spec/最近可核实证据/本地与外部状态/缺口/本次包号”。逐条核对046 T0–T10验收点与047声明，核销已完成045，不重新启动旧计划。旧计划中尚待现场观察的事项保持未验收，不能因为日期已过去自动关闭。

重点核查：047锚1327与机器源1295的差异；全量11项实际覆盖范围；工作台专项是否只有API造数+空表；Oracle测试究竟是方言/驱动可用性还是真实DB执行。给每个skip标“按设备分工/外部依赖/缺失覆盖”，不要求合理的设备分工skip清零。

架构输出用当前组件与数据流说明事务边界、权限边界、应用库/业务库/预检库分离；仅对与本批问题直接相关的模块建议局部调整。避免泛泛的“拆微服务/提升性能/增加监控”。

验收：矩阵每项可追溯；新增问题注明静态嫌疑还是动态复现；本次范围外的问题放优先队列，不扩大实施白名单。

### T2 — 工作台分页、聚合与数据边界

1. 为checks增加受校验的分页参数，默认page=1、page_size=100，上限300；保留旧limit调用兼容，并定义同时传入时page_size优先。非法page/size/is_trial返回可预期4xx，不返回500。
2. total表示经过全部权限与筛选后的匹配run数，items为当前页；排序至少created_at + 唯一ID，时间相同不能使翻页重复/漏行。新响应字段为additive，不移除旧字段。
3. 在执行方核验BFF科室范围、service actor和预检侧权限契约后，保证列表、total、详情和关联计数一致限制在授权范围；不得仅前端隐藏。跨科室相同patient_id、不同visit_number仍精确隔离。
4. 消除读取全部IssueRow：只查询当前页涉及且授权可见的就诊/正式或trial范围，使用分组聚合或等价有界查询；零本页run不扫全库。明确现有计数是“就诊当前缺陷状态”，不伪称历史run时点快照；如现行契约证实为其他语义，记录依据并保持兼容。
5. 以现有服务层惯例提取必要查询函数，路由保留认证和参数处理，不重写整个closed_loop_api。

验收：合成数据至少覆盖301条run、相同时间、跨科室、相同患者不同次数、空页、筛选后不足一页、非法参数；翻页集合无重复无漏项，total正确，未授权count也不泄漏。用SQL事件/查询结果行数证明不全量加载Issue，记录100/1000/10000行数据下耗时与环境，不设置无依据固定毫秒阈值。SQLite真实执行+Oracle方言检查不等于Oracle生产验收。

### T3 — 两套前端工作台成为可连续使用的页面

1. UI Next与legacy接入T2分页、total、刷新和筛选重置；查询条件变化回第一页。显示患者ID+次数、科室、时间和当前页范围，空态与错误态区分。
2. 刷新/重试入口放在错误条件外。列表、详情、trial列表、trial观察分别管理loading/error；失败不残留上一患者或上一trial内容。旧请求晚返回不得覆盖新选择。
3. 依据已有有效权限控制查看/反馈/终态复核操作，调查项目已有权限读取来源并复用；缺权限默认不展示可执行按钮，后端403仍是最终门。管理员特殊策略沿用后端，不创造新授权。
4. 显示五态及人工状态中文标签，未知枚举安全回退；reason_code映射已知原因并可查看技术码，业务页面去掉不必要的BFF等实现术语。区分人工复核与引擎结论，不把“复检通过”按钮包装成已重新运行引擎。
5. 防重复提交；409提示数据已更新并刷新版本，403提示无权，502/503提示服务暂不可用并可重试。只有现有契约要求原因的动作强制原因，不增加多余二次确认。
6. 1366桌面和390移动宽度检查：工具栏换行、详情可滚动、操作可点击、键盘可达。复用当前Element Plus样式，不做全站视觉改版。

验收：组件或E2E验证初次失败→重试成功、详情失败、快速切换详情、trial失败、409刷新、无权限按钮、中文状态和分页；至少一条非空合成就诊从列表→详情→已查看→提交整改→有权限复核/误报，断言人工状态和引擎结果分别保留。

### T4 — 匹配任务恢复与事务健壮性

1. 先阅读现有MatchService与测试，不假设接口没有真实模型适配器。真实通道本次不调用。
2. 对候选持久化之后、进度记录之前的崩溃注入，以及同task重复/并发run、cancel、任务不存在、模型timeout/非法JSON进行合成测试。
3. 已复现问题在现有schema内优先用原子事务/条件状态迁移修复：候选+FID完成记录保持一致，重试不重复、不丢失已成功FID；消失任务返回明确结果而非refresh(None)异常。失败FID是否允许重试遵循现行契约，不擅自重跑已接受候选。
4. 模型调用不长时间占用数据库写事务；取消与完成竞争不得将cancelled覆盖为completed；不得吞异常后假报成功。无法在当前schema安全实现的部分列具体阻塞，不用只支持单线程的实现冒充持久化幂等。

验收：给出失败复现→修复后通过证据；失败注入至少覆盖事务中断/取消/重复执行三类。原本已通过的保证可复用既有测试node，报告实际验证范围。无发现则提交证据而不是无意义改代码。

### T5 — 高风险功能交界与前后端专项验收

1. 依据T1矩阵复用既有测试验证主服务RBAC/BFF白名单、跨科室列表和详情、JHEMR未签名/重放/过期票据/错误就诊/幂等、Outbox目标停用/失败重试/重复补偿、trial不进入正式结果/推送。
2. 检查旧AI质控manual/bulk/serial隔离、双调度独立锁、日志历史NULL与导出审计、Relay部分保存与suppression仍有有效覆盖；只对本批影响且缺失的边界补测，不重写已稳定主链。
3. 工作台正向测试禁止整体豁免prearchive-admin失败。建立精确的请求方法+路径+状态预期；冲突测试的409只允许该请求，降级轮只允许规定502/503；/users/me只保留已知未认证探测例外。断言零未处理pageerror。
4. 将workbench.spec正向与降级纳入本专项可重复编排；UI Next有非空工作台真后端链路，不能仅mock页面或仅API造数后看空表。可新增专项CLI开关，不改变普通full默认约定；本048终验必须显式运行所有专项。
5. JHEMR L1优先复用现有mock_client和fixtures复跑；若没有稳定自动编排，在本地专项脚本加入自有服务启动、健康探测、失败日志和finally清理。不同栈串行，禁止为测试调用生产。

验收：规则中心legacy双轮、工作台legacy双轮、UI Next非空工作台、JHEMR L1各有独立结果和命令；负向用例明确预期拒绝。测试执行不依赖手工造真实患者或外部模型凭据。

### T6 — 全量门禁与证据完整性

1. 正式入口：`python scripts/run_gates_20260906.py --full --anchor-file docs/reference/gate_anchors.json`。按demo Skill先启动本任务demo主服务，聚合脚本自行管理sidecar；不预启动占用其端口的外部sidecar。
2. 同时完成T5中尚未纳入full的专项命令，在049单列；full exit0不代表专项完成。构建会同步static，保存本任务生成diff并验证产物入口一致。
3. 以Markdown汇总+JSON+原始日志交叉核查PASS/FAIL/SKIPPED、实际测试数与专项覆盖。锚文件存在且有效才可声明比锚成功；本次不自动更新锚，不把047误写数字复制进机器源。
4. 比较T0与最终的用例集合和skip原因，不单靠passed总数；新增覆盖抵消不了旧关键测试丢失。失败只暂停依赖工作，定位并修复范围内原因后复跑必要项；最终代码变化影响全量结论时须重新全量。
5. 新增专项若无法运行，报告BLOCKED及原因，本048不得标完成。合理设备分工skip、未授权Oracle现场测试可保留，并明确不属于本地验收覆盖。

验收：所有本地必做门与专项真实通过，旧回归不丢失、未降低断言或锚；已知外部验收单列，不伪装成PASS。

### T7 — 交付、回退与下一次免费额度安排

1. 创建/更新同一049：执行摘要、T0–T7逐包状态、T1全系统矩阵、逐缺陷证据、修改清单、测试命令与结果、可点击页面路径、剩余风险、回退步骤、外部未完成清单。
2. 外部清单逐项写：具体工作、负责方、必需输入、批准要求、成功证据。至少包含JHEMR L2/L3、真实AI通道/模型与目录脱敏范围、真实回测样本/临床核验、W10规则来源、生产DBA迁移与备份、UI Next真实角色canary。不填造负责人姓名或日期。
3. 047仅追加本次核验更正：危险“丢弃整个工作区”回退建议失效、锚数差异与覆盖边界；历史数字原文不改。新回退方案以T0文件副本/hash和本次diff为单位，保留其他会话改动；不得git reset/clean/stash或覆盖未知变更。
4. 在023§0.6追加本地专项状态，INDEX同步048/049状态/路径/更新时间，README入口同步，01追加实际事实与证据。未全部闭包时不能标“已执行完毕”。
5. finally停止本任务启动的demo/mock/预览进程，核验端口；不销毁他人目录、不杀未知PID；保留本次合成证据。Git写操作默认不执行，交付“本地完成，未提交”。

验收：用户可用049复核结果及下一步，不需翻完整聊天；如有未完成包，给出包号、失败命令、最后hash和准确续跑点。

## 4. 执行预算与防止任务膨胀

建议精力分配：T0/T1约15%，T2/T3约35%，T4约15%，T5/T6约25%，T7约10%。比例是调度建议，不是超时即停规则。不要在已定位的小问题上反复跑全量，不用大规模重构消耗额度。

本次之后的候选主题按依赖排序：①若本地包残留则先续跑同049；②获得外部资料后做JHEMR L2联合验收；③院内真实匹配+trial观察与临床抽检；④生产迁移/试点及回滚验证（必须单独批准）；⑤再依据T1矩阵选择全站前端或性能专项。外部资料未到时选已取证的本地缺口，避免每天生成另一份泛化检查报告。

## 5. 完成定义

- [ ] T0现场与隔离环境留证，既有修改保留。
- [ ] T1全系统与历史任务矩阵有实现/测试/证据定位。
- [ ] T2分页total与有界聚合、T3双前端可恢复操作通过。
- [ ] T4故障复现与事务边界已验证，复现缺陷已修；实质阻塞未伪称完成。
- [ ] T5规定的真后端专项全部运行，精确失败断言通过。
- [ ] T6全量+专项通过、skip分类、锚未被擅改。
- [ ] T7报告、023/INDEX/README/01、回退与进程收尾齐全。
- [ ] 生产、外部模型、临床验收与Git状态明确，不以本地mock替代外部完成。

## 6. 编制阶段验证

本节仅记录本次计划文件的检查，不代表T0–T7已执行。编制完成后运行quick及文档引用/编号/差异检查，结果登记01；执行AI仍须重新冻结自己的基线。

编制验证（2026-09-15）：`review/gate-runs/20260915-181500/summary.md`，quick四项PASS、exit0；T0–T7章节、INDEX/README/023/PROMPT关联、049仅预留检查通过，相关已跟踪文档git diff --check通过。本次未跑full、未实施修复、未访问生产、未提交Git。
