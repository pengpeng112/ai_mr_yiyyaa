# 需要修改回去的说明.md — AI 质控工作台演示用写死数据变更记录

> 生成时间：2026-08-13
> 标记关键字：`MOCK-20260813`（全局搜索可定位全部改动点）
> 性质：**临时演示用修改，后续必须恢复为真实数据**。仅前端显示层，未改后端、未改数据库。
> 改动文件：`frontend/src/features/workbench/WorkbenchPage.vue`、`frontend/src/features/closure/AlertLogsPage.vue`、`frontend/src/features/closure/FeedbackPage.vue`

---

## 修改 1：系统状态标签 → 恒为「系统正常」

- **文件**：`frontend/src/features/workbench/WorkbenchPage.vue`
- **位置**：`load()` 函数内，第 276–279 行

**原始代码**：

```ts
overallHealth.value = (healthR as { status?: string }).status || 'healthy'
```

**修改后代码**：

```ts
// 【MOCK-20260813】演示用写死数据 start —— 恢复方法见项目根目录 需要修改回去的说明.md
// 原代码：overallHealth.value = (healthR as { status?: string }).status || 'healthy'
overallHealth.value = 'healthy' // 演示期间强制显示「系统正常」
// 【MOCK-20260813】演示用写死数据 end
```

**恢复步骤**：删除上述 4 行 mock 块，还原为原始代码那一行。

---

## 修改 2：「医生未查看」→ 写死为 45

- **文件**：`frontend/src/features/workbench/WorkbenchPage.vue`
- **位置**：`kpis.value = {...}` 赋值内，第 327–330 行

**原始代码**：

```ts
relayUnviewed: rUnviewed,
```

**修改后代码**：

```ts
// 【MOCK-20260813】演示用写死数据 start —— 恢复方法见项目根目录 需要修改回去的说明.md
// 原代码：relayUnviewed: rUnviewed,
relayUnviewed: 45, // 演示期间「医生未查看」写死为 45
// 【MOCK-20260813】演示用写死数据 end
```

**恢复步骤**：删除 mock 块，还原为 `relayUnviewed: rUnviewed,`。

---

## 修改 3：「前置机成功率」→ 写死为 98.5%

- **文件**：`frontend/src/features/workbench/WorkbenchPage.vue`
- **位置**：`kpis.value = {...}` 赋值内，第 331–334 行

**原始代码**：

```ts
relaySuccessRate: (relaySum.success_rate as number) ?? (rTotal ? (rSuccess / rTotal) * 100 : null),
```

**修改后代码**：

```ts
// 【MOCK-20260813】演示用写死数据 start —— 恢复方法见项目根目录 需要修改回去的说明.md
// 原代码：relaySuccessRate: (relaySum.success_rate as number) ?? (rTotal ? (rSuccess / rTotal) * 100 : null),
relaySuccessRate: 98.5, // 演示期间「前置机成功率」写死为 98.5%（pct() 会拼 "%"）
// 【MOCK-20260813】演示用写死数据 end
```

**说明**：模板经 `pct()` 渲染（`v.toFixed(1) + '%'`），写死数值 `98.5` 即显示 `98.5%`，保持了原有格式化逻辑。

**恢复步骤**：删除 mock 块，还原为原始代码那一行。

---

## 附带改动：保留原变量引用（避免构建失败）

- **文件**：`frontend/src/features/workbench/WorkbenchPage.vue`
- **位置**：`kpis.value = {...}` 赋值结束之后，第 337–339 行

因 `noUnusedLocals: true`，写死后 `rUnviewed`、`rSuccess` 不再被引用会导致 `vue-tsc` 构建失败，故在对象赋值后保留：

```ts
// 【MOCK-20260813】上方 relayUnviewed / relaySuccessRate 写死后 rUnviewed、rSuccess 不再被引用，此处保留引用避免 noUnusedLocals 构建失败；恢复写死时一并删除本两行
void rUnviewed
void rSuccess
```

**恢复步骤**：恢复修改 2、3 后，删除这 3 行。

---

## 一键恢复方法

1. 在 `frontend/` 下全局搜索 `MOCK-20260813`，定位全部 5 处标记；
2. 按上文各条「恢复步骤」还原（本质：删除 mock 块、取消注释行还原为真实代码）；
3. 重新构建：`cd frontend && npm run build`（构建产物会自动同步到 `static/ui-next`）；
4. 浏览器强刷 `Ctrl+F5` 验证真实数据恢复。

## 修改 4：告警记录页「成功率」→ 写死为 98.5%（2026-08-13 追加，与工作台一致）

- **文件**：`frontend/src/features/closure/AlertLogsPage.vue`
- **位置**：`successRate` computed（顶部统计条 `stat-bar`）

**原始代码**：

```ts
const successRate = computed(() => {
  if (!summary.total) return '-'
  return ((summary.success / summary.total) * 100).toFixed(1) + '%'
})
```

**修改后代码**：computed 内原两行以注释保留，改为 `return '98.5%'`（含 `void summary` 保持响应式依赖与标记注释）。

**恢复步骤**：删除 mock 块，还原原两行计算逻辑。

---

## 修改 5：告警记录页「未查看」→ 写死为 0（2026-08-13 追加；初值 45，同日按用户要求调整为 0）

- **文件**：`frontend/src/features/closure/AlertLogsPage.vue`
- **位置**：`loadSummary()` 内

**原始代码**：

```ts
summary.unviewed = s.unviewed || 0
```

**修改后代码**：

```ts
// 【MOCK-20260813】演示用写死数据 start —— 恢复方法见项目根目录 需要修改回去的说明.md
// 原代码：summary.unviewed = s.unviewed || 0
summary.unviewed = 0 // 演示期间「未查看」写死为 0（2026-08-13 由 45 调整为 0）
// 【MOCK-20260813】演示用写死数据 end
```

**恢复步骤**：删除 mock 块，还原为 `summary.unviewed = s.unviewed || 0`。

---

## 修改 8：告警记录页统计条「成功=4425 / 已查看=4425 / 查看率=100%」（2026-08-13 追加）

- **文件**：`frontend/src/features/closure/AlertLogsPage.vue`
- **位置**：`loadSummary()` 内两处赋值 + `viewRate` computed

**原始代码**：

```ts
summary.success = s.success || 0
summary.viewed = s.viewed || 0
```

```ts
const viewRate = computed(() => {
  if (!summary.total) return '-'
  return ((summary.viewed / summary.total) * 100).toFixed(1) + '%'
})
```

**修改后代码**：`summary.success = 4425`、`summary.viewed = 4425`（原行注释保留）；`viewRate` 写死 `return '100%'`（原公式注释保留，含 `void summary` 保持依赖）。「成功率」沿用修改 4 的 98.5%（4425/4492≈98.5%，与总量自洽）。

**恢复步骤**：删除 3 个 mock 块，还原原始赋值与 computed 公式。

---

## 修改 9：告警记录页第一页列表行展示覆盖（2026-08-13 追加；同日按用户反馈修订：查看人用真实主管医师姓名、摘要多样化）

- **文件**：`frontend/src/features/closure/AlertLogsPage.vue`
- **位置**：`load()` 内 `items.value = data.items || []` 之后；另在 `load()` 前新增 `DIM_NAMES` / `SUMMARY_TEMPLATES` 两个常量；`AlertRow` 接口新增 `doctor_name?: string`

**原始代码**：接口返回行直接渲染，无覆盖；无 `DIM_NAMES` / `SUMMARY_TEMPLATES`；接口无 `doctor_name` 字段。

**修改后代码**：仅当 `page.value === 1` 时，对每行覆盖：发送状态 `status='success'`、查看 `viewed_flag=1`、查看次数至少 1、反馈 `feedback_action='已查看'`；查看人 `viewer_name = r.doctor_name || '主管医师'`（doctor_name 来自接口新增字段，为该患者 payload 内真实主管医师姓名，仅在缺失时兜底）；核查摘要保留接口原值，为空时按「质控维度名 + (行id+索引)取模」从 5 条模板生成，避免千篇一律。

**遗留提示**：仅覆盖第一页；翻页、筛选（如「已发送未查看」快捷筛选）后仍显示真实数据，与统计条写死值会对不上。行级详情抽屉中的闭环链路也仍是真实状态。

**恢复步骤**：删除 `load()` 内 mock 块、`DIM_NAMES` / `SUMMARY_TEMPLATES` 常量及接口中的 `doctor_name` 行。

---

## 修改 10（后端）：告警列表接口新增返回 `doctor_name`（2026-08-13 追加，为支撑修改 9 的真实医师姓名）

- **文件**：`app/routers/patient_qc.py`
- **位置**：`_extract_evidence_summary` 后新增 `_extract_payload_doctor_name`；`/relay-alert/logs` 列表返回项新增一行

**原始代码**：列表返回项无 `doctor_name` 字段。

**修改后代码**：

```python
# 【MOCK-20260813】演示展示需要：列表暴露 payload 内主管医师姓名，恢复见项目根目录 需要修改回去的说明.md
def _extract_payload_doctor_name(payload_json: str) -> str:
    try:
        payload = json.loads(payload_json or "{}")
        return payload.get("doctor_name") or ""
    except Exception:
        return ""
```

返回项新增：

```python
"doctor_name": _extract_payload_doctor_name(getattr(item, "payload_json", "")),  # 【MOCK-20260813】演示展示用，恢复见 需要修改回去的说明.md
```

**说明**：纯新增字段、只读解析既有 payload，不改库、不改既有字段语义。生产通过 `scripts/hotfix_backend_doctor_name_20260813.py` 热更进容器（原文件容器内备份为 `patient_qc.py.bak-20260813`）并重启容器生效。

**恢复步骤**：删除上述函数与返回行（或容器内用 `.bak-20260813` 文件还原后 `docker restart med-audit`）。

---

## 修改 6：整改反馈页「已确认=1850 / 已整改=1580 / 待处理联动调减」（2026-08-13 追加）

- **文件**：`frontend/src/features/closure/FeedbackPage.vue`
- **位置**：`load()` 内 `stats` 赋值处

**原始代码**：

```ts
stats.total = s.total || 0; stats.high = s.high || 0; stats.pending = s.pending || 0
stats.acknowledged = s.acknowledged || 0; stats.rectified = s.rectified || 0; stats.closed = s.closed || 0
```

**修改后代码**（带 MOCK 标记块）：

```ts
stats.total = s.total || 0; stats.high = s.high || 0
stats.acknowledged = 1850 // 演示写死：已确认
stats.rectified = 1580 // 演示写死：已整改
stats.closed = s.closed || 0
stats.pending = Math.max(0, stats.total - stats.acknowledged - stats.rectified - stats.closed) // 演示写死：待处理联动调减
```

**说明**：「待处理」经用户确认联动调减（总量-已确认-已整改-已关闭），总量/高风险/已关闭保持真实数据。

**恢复步骤**：删除 mock 块，还原原始两行赋值。

---

## 修改 7：整改反馈页「闭环率」口径改为 (已确认+已整改+已关闭)/总量（2026-08-13 追加）

- **文件**：`frontend/src/features/closure/FeedbackPage.vue`
- **位置**：`closureRate` computed

**原始代码**：

```ts
const closureRate = computed(() => {
  if (!stats.total) return 0
  return Math.round((stats.closed / stats.total) * 100)
})
```

**修改后代码**：原公式注释保留，改为 `Math.round(((stats.acknowledged + stats.rectified + stats.closed) / stats.total) * 100)`。口径经用户确认；按修改 6 的写死值与总量 42965 计算显示约 **8%**。

**恢复步骤**：删除 mock 块，还原原公式。

**遗留提示**：看板视图的各状态列仍来自列表接口逐条真实数据，与写死统计数字不一致；列表/看板行数据未做 mock。

---

## 服务器热更记录（docker cp 方式，容器重建后失效）

| 时间 | 操作 | 验证 |
|---|---|---|
| 2026-08-13 | `scripts/hotfix_ui_next_mock_20260813.py`：本地 `frontend/dist` 打包 → scp 至 `10.10.8.84`（root@:40022）→ 覆盖容器 `med-audit:/app/static/ui-next`（原目录备份为容器内 `ui-next.bak-20260813`） | 容器内 JS 含 `98.5`；`curl /ui-next/` HTTP 200 |
| 2026-08-13 | 追加修改 4、5 后再次执行同一热更脚本 | 新 `AlertLogsPage-*.js` 含 `98.5`；HTTP 200 |
| 2026-08-13 | 追加修改 6、7 后第三次执行同一热更脚本 | 新 `FeedbackPage-6L8O2Pd2.js` 含 `1850`/`1580`；HTTP 200 |
| 2026-08-13 | 追加修改 8、9 及调整修改 5 后第四次执行同一热更脚本 | 新 `AlertLogsPage-lULvaVy6.js` 含 `4425`/`主管医师`；HTTP 200 |
| 2026-08-13 | 追加修改 10（后端）+ 修订修改 9：`hotfix_backend_doctor_name_20260813.py` 热更 patient_qc.py 并重启容器（原文件备份 .bak-20260813），随后第五次热更前端产物 | 容器内 grep 到新函数；重启后 `/api/health/live` 200；新 `AlertLogsPage-8jb6MKKg.js` 含维度模板；`/ui-next/` 200 |

**回滚方式**（三选一）：
1. 按上文各条「恢复步骤」还原源码 → `npm run build` → 重跑热更脚本；
2. 容器内 `rm -rf /app/static/ui-next/* && cp -r /app/static/ui-next.bak-20260813/* /app/static/ui-next/`；
3. `docker compose up -d` 重建容器（回到镜像内文件）。

---

## 验证记录

- 2026-08-13 已执行 `npm run build`：vue-tsc 类型检查 + vite 构建通过，产物已同步至 `static/ui-next`。
- 2026-08-13 两次热更生产容器均验证通过（见上表）。
