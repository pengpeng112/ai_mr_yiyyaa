# 028 一期预检服务独立依赖（与主服务 requirements 完全分离；pyodbc 只出现在这里）
# 目标运行环境：Python 3.11+（本地原型在 3.14 验证）
#
# 注意：cx_Oracle / psycopg2-binary / pyodbc 仅在真实四源模式需要；
# fixture 模式与全部单元测试不需要安装这三个驱动（真实网关惰性建连）。
# 生产容器供应链注意 unixODBC + MS ODBC Driver（028 R11）。

SQLAlchemy>=2.0
fastapi>=0.110
uvicorn>=0.29
pydantic>=2.6
cryptography>=42.0
pytest>=8.0
httpx>=0.27          # FastAPI TestClient 依赖

# —— 真实四源驱动（按需安装；Linux 容器注意 R11）——
psycopg2-binary>=2.9
cx_Oracle>=8.3
pyodbc>=5.0
